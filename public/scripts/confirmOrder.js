/*replaceWeirdID: function(weirdId){
  var res = str.replace(/_x3/i, weirdId);
  res = str.replace(/__[0-9]_/i, res);
  return res;
}*/


'use strict';

define(["jquery",
		"compose"],
	function($,
		compose){

	var confirmOrder = compose( {

		
	});

	return confirmOrder;
});
